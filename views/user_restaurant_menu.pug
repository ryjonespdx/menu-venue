extends layout

block content

  h4= menu.name

    dl
      each item in menu
        dt #{item.name}
        dd #{item.price}
        dd #{item.description}
        dd
          a(href='/user/'+user_info.username+'/restaurant/'+restaurant_info.id+'/menu/'+menu_info.id+'/item/'+item.id+'/update') update item 

      else
        p You have no items.

      a(href='/user/'+user_info.username+'/restaurant/'+restaurant_info.id+'/menu/'+menu_info.id+'/item/create') create item
